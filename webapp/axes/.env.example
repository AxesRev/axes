APP_BASE_URL=http://localhost:3000
AUTH0_DOMAIN=your-tenant.us.auth0.com
AUTH0_CLIENT_ID=
AUTH0_CLIENT_SECRET=
AUTH0_SECRET=
AEGRA_API_URL=http://127.0.0.1:8000
# Tenant API Gateway invoke URL (Lambda). Dashboard /tenants/me* routes.
TENANT_API_URL=
# Billing API Gateway invoke URL (Lambda). Same INTERNAL_API_SECRET as billing.
BILLING_API_URL=
INTERNAL_API_SECRET=

# Paddle Billing (sandbox only)
# Client token: Paddle > Developer tools > Authentication > Client-side tokens (starts with test_)
NEXT_PUBLIC_PADDLE_CLIENT_TOKEN=
# $0/month recurring price for card-on-file setup (real pri_ ID from sandbox catalog)
NEXT_PUBLIC_PADDLE_BASE_PRICE_ID=
# In Paddle sandbox: Checkout > Checkout settings > Default payment link = http://localhost:3000
